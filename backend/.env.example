# Clairety Backend Environment Variables — Template
# Rename this file to `.env` and fill in the values before running the server.

PORT=3000

# Future: Add backend/server-side API keys here
# DATABASE_URL=your_database_url
# GOOGLE_VISION_API_KEY=your_google_key
# STRIPE_SECRET_KEY=your_stripe_key
